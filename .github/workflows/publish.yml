# This workflow has been merged into bump.yml.
# Publishing now happens automatically after version bump on push to main.
# This file can be safely deleted.

name: Publish (deprecated)

on:
  workflow_dispatch: # manual trigger only, as a fallback

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:

      - name: Check-out repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set-up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Set-up Poetry
        run: pip install "poetry>=2.0,<3.0"

      - name: Set project version
        run: |
          poetry version $(git describe --tags --match "v[0-9]*" --abbrev=0)

      - name: Configure Poetry for PyPI
        run: |
          poetry config http-basic.pypi "__token__" ${{ secrets.PYPI_TOKEN }}

      - name: Publish to PyPI
        run: poetry publish --build
