version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - method: pip
    path: .
    - extra_requirements:
        - tests
        - docs

sphinx:
  configuration: docs/conf.py  # Replace with the path to your Sphinx conf.py
